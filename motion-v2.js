'use strict';
const VERSION='2.0.0';
const STORAGE_KEY='motion404-v2-state';
const MAX_SAVED=50;
const PAGE_SIZE=18;
const MAX_IMPORT_BYTES=1000000;

const industries=['Arquitectura','IA y software','Restauración','Moda','Música','Videojuegos','Viajes','Finanzas','Salud y bienestar','Educación','Inmobiliaria','Fotografía','Automoción','Cultura','E-commerce','Consultoría','Deporte','Sostenibilidad'];
const experiences=['Landing page','Aplicación web','Portfolio','E-commerce','Dashboard','Editorial','Experiencia 3D'];
const stacks=['HTML + CSS + JS','React + CSS Modules','React + Tailwind','React + Framer Motion','React + GSAP','Three.js + GSAP'];
const visuals=['Brutalismo editorial','Futurismo orgánico','Minimalismo cinético','Neo-retro digital','Lujo monocromo','Tecnología translúcida','Collage tipográfico','Industrial táctil','Gradiente espacial','Editorial mediterráneo'];
const motions=['Microinteracciones suaves','Scroll cinematográfico','Parallax multicapa','Tipografía cinética','Transiciones de máscara','Objetos 3D reactivos','Cursor magnético','Revelado secuencial'];
const categories=['Landing','Aplicación','Portfolio','E-commerce','Dashboard','Editorial','Experiencia 3D','Arquitectura','Restauración','Gaming'];
const suffixes=['Core','Flow','Studio','Motion','Canvas','XR'];
const accents=['#ff6b35','#9d7cff','#70d6ff','#76e39d','#f4c95d','#ff7aa8'];

const DNA_PROFILES={
  cinematic:{label:'Cinematic',easing:'cubic-bezier(.22,.61,.36,1)',duration:'520–900 ms',stagger:'70–120 ms',character:'Narrativo, inmersivo y deliberado',enter:'Máscara + profundidad leve',hover:'Microdesplazamiento + luz',scroll:'Secciones con continuidad y progresión',avoid:'Parallax simultáneo en demasiadas capas'},
  minimal:{label:'Minimal',easing:'cubic-bezier(.2,.8,.2,1)',duration:'180–360 ms',stagger:'30–60 ms',character:'Preciso, silencioso y funcional',enter:'Opacity + translate 8–16 px',hover:'Contraste, escala ≤1.02',scroll:'Revelados discretos',avoid:'Decoración cinética sin función'},
  kinetic:{label:'Kinetic',easing:'cubic-bezier(.16,1,.3,1)',duration:'240–520 ms',stagger:'24–48 ms',character:'Energético, tipográfico y rítmico',enter:'Split/reveal tipográfico',hover:'Respuesta elástica contenida',scroll:'Ritmo marcado por copy y CTA',avoid:'Animar párrafos largos carácter a carácter'},
  editorial:{label:'Editorial',easing:'cubic-bezier(.25,.46,.45,.94)',duration:'320–620 ms',stagger:'50–90 ms',character:'Jerárquico, cultural y legible',enter:'Revelado por bloques',hover:'Subrayado, crop o shift',scroll:'Capítulos y cambios de escala',avoid:'Romper lectura con scroll hijacking'},
  brutal:{label:'Brutalist',easing:'steps(1,end)',duration:'90–220 ms',stagger:'0–30 ms',character:'Directo, gráfico y abrupto',enter:'Cuts y cambios de estado',hover:'Inversión, borde, desplazamiento seco',scroll:'Cambios de bloque explícitos',avoid:'Mezclar brutalismo con easing flotante continuo'},
  spatial:{label:'Spatial',easing:'cubic-bezier(.19,1,.22,1)',duration:'450–850 ms',stagger:'60–110 ms',character:'Profundo, físico y reactivo',enter:'Z-depth + scale controlado',hover:'Tilt ≤4° + parallax local',scroll:'Cámara o capas con límite',avoid:'3D continuo en móvil sin fallback'}
};
const dnaKeys=Object.keys(DNA_PROFILES);
const concepts=[
 ['Atlas Habitat','Arquitectura','Arquitectura','Estudio de arquitectura con proyectos inmersivos y planos editoriales.'],['Neural Atlas','Landing','IA y software','Producto IA que traduce datos complejos a una narrativa visual clara.'],['Mesa Nómada','Restauración','Restauración','Restaurante de cocina viajera con carta visual y reservas.'],['Nocturne Records','Editorial','Música','Sello independiente con lanzamientos, artistas y escucha editorial.'],['Void Runner','Gaming','Videojuegos','Videojuego sci-fi con universo narrativo y llamada a la acción.'],['Asteria Journeys','Landing','Viajes','Viajes de autor con destinos inmersivos y storytelling fotográfico.'],['Lumen Capital','Dashboard','Finanzas','Panel financiero sobrio con métricas y escenarios accionables.'],['Pulse Studio','Aplicación','Salud y bienestar','App de hábitos y entrenamiento con progreso y rutinas.'],['Forma Academy','Aplicación','Educación','Plataforma educativa con itinerarios y seguimiento.'],['Nexo Living','E-commerce','Inmobiliaria','Catálogo inmobiliario premium con filtros y visitas.'],['Grain & Light','Portfolio','Fotografía','Portfolio fotográfico con ritmo editorial y series.'],['Torque Lab','Landing','Automoción','Laboratorio de movilidad con lenguaje industrial.'],['Archivo Vivo','Editorial','Cultura','Archivo cultural con exposiciones y cronologías.'],['Serein Objects','E-commerce','E-commerce','Tienda de objetos de diseño con fotografía protagonista.'],['Northbound','Landing','Consultoría','Consultora estratégica con tesis claras y casos.'],['Kinetic Club','Aplicación','Deporte','App deportiva con retos, equipos y resultados.'],['Terra Loop','Landing','Sostenibilidad','Iniciativa climática con impacto medible.'],['Mono Studio','Portfolio','Arquitectura','Portfolio minimalista de procesos, maquetas y obra.'],['Signal OS','Aplicación','IA y software','Productividad con comandos y panel modular.'],['Fire & Ferment','Restauración','Restauración','Experiencia gastronómica de fuego, fermentos y origen.'],['Eclipse Festival','Landing','Música','Festival con programación, artistas y entradas.'],['Arcadia Bestiary','Gaming','Videojuegos','Enciclopedia fantástica con criaturas, mapas y lore.'],['Drift House','Experiencia 3D','Viajes','Refugio remoto presentado como recorrido 3D contemplativo.'],['Flux Ledger','Dashboard','Finanzas','Dashboard fintech con cartera y simulación de riesgo.'],['Still Mind','Aplicación','Salud y bienestar','App de respiración y descanso con estadísticas discretas.'],['Orbit Learn','Aplicación','Educación','Aprendizaje espacial basado en mapas de conocimiento.'],['Casa Clara','Arquitectura','Inmobiliaria','Promoción residencial mediterránea con planos.'],['Rare Frames','Portfolio','Fotografía','Portfolio cinematográfico con proyectos y making-of.'],['Volta EV','Experiencia 3D','Automoción','Configurador conceptual de vehículo eléctrico.'],['Museo 404','Editorial','Cultura','Museo digital experimental con salas temáticas.']
];

const $=(s,r=document)=>r.querySelector(s); const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const el={grid:$('#presetGrid'),count:$('#presetCount'),empty:$('#presetEmpty'),search:$('#presetSearch'),category:$('#categoryFilter'),dnaFilter:$('#dnaFilter'),load:$('#loadMorePresets'),form:$('#systemForm'),intensity:$('#intensityRange'),intensityOut:$('#intensityOutput'),output:$('#outputPanel'),outputTitle:$('#output-title'),outputMeta:$('#outputMeta'),dnaPanel:$('#panel-dna'),spec:$('#specOutput'),timeline:$('#timelineOutput'),qaScore:$('#qaScore'),qaList:$('#qaList'),riskBadge:$('#riskBadge'),riskRing:$('#riskRing'),riskScore:$('#riskScore'),perfRisk:$('#perfRisk'),cognitiveRisk:$('#cognitiveRisk'),a11yRisk:$('#a11yRisk'),riskSummary:$('#riskSummary'),recs:$('#liveRecommendations'),savedGrid:$('#savedGrid'),savedEmpty:$('#savedEmpty'),toast:$('#toast')};
let visible=PAGE_SIZE; let currentSystem=null; let currentTab='dna'; let toastTimer;
let persisted=loadState();

function loadState(){try{const raw=localStorage.getItem(STORAGE_KEY);if(!raw)return{theme:'dark',saved:[]};const data=JSON.parse(raw);return{theme:data.theme==='light'?'light':'dark',saved:Array.isArray(data.saved)?data.saved.filter(validSaved).slice(0,MAX_SAVED):[]}}catch{return{theme:'dark',saved:[]}}}
function validSaved(item){return item&&typeof item==='object'&&typeof item.id==='string'&&typeof item.project==='string'&&item.project.length<=80&&item.output&&typeof item.output==='object'}
function saveState(){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(persisted));return true}catch{return false}}
function toast(message){clearTimeout(toastTimer);el.toast.textContent=message;el.toast.classList.add('show');toastTimer=setTimeout(()=>el.toast.classList.remove('show'),2600)}
function esc(v){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function clamp(n,min,max){return Math.min(max,Math.max(min,n))}
function slug(v){return String(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'').slice(0,60)||'motion-system'}

function createPresets(){const out=[];for(let round=0;round<6;round++){concepts.forEach((c,i)=>{const[base,category,industry,description]=c;const dna=dnaKeys[(i+round)%dnaKeys.length];const intensity=1+((i+round*2)%5);const pace=['calm','balanced','fast'][(i+round)%3];const depth=['flat','layered','spatial'][(i+round*2)%3];out.push({id:`m404-${String(round*concepts.length+i+1).padStart(3,'0')}`,title:round?`${base} ${suffixes[round]}`:base,category,industry,description,dna,intensity,pace,depth,visual:visuals[(i+round*2)%visuals.length],stack:stacks[(i+round)%stacks.length],motion:motions[(i*2+round)%motions.length],accent:accents[(i+round)%accents.length]})})}return out}
const presets=createPresets();

function fillOptions(){const set=(id,values,mapper=v=>[v,v])=>{const s=$(id);s.innerHTML=values.map(v=>{const[a,b]=mapper(v);return`<option value="${esc(a)}">${esc(b)}</option>`}).join('')};set('#experienceSelect',experiences);set('#industrySelect',industries);set('#stackSelect',stacks);set('#visualSelect',visuals);set('#dnaSelect',dnaKeys,k=>[k,DNA_PROFILES[k].label]);el.category.innerHTML='<option value="all">Todas</option>'+categories.map(v=>`<option>${esc(v)}</option>`).join('');el.dnaFilter.innerHTML='<option value="all">Todos</option>'+dnaKeys.map(k=>`<option value="${k}">${DNA_PROFILES[k].label}</option>`).join('')}

function filteredPresets(){const q=el.search.value.trim().toLowerCase();return presets.filter(p=>(!q||`${p.title} ${p.category} ${p.industry} ${p.visual} ${p.stack} ${DNA_PROFILES[p.dna].label}`.toLowerCase().includes(q))&&(el.category.value==='all'||p.category===el.category.value)&&(el.dnaFilter.value==='all'||p.dna===el.dnaFilter.value))}
function renderPresets(){const list=filteredPresets();const shown=list.slice(0,visible);el.grid.innerHTML=shown.map(p=>`<article class="preset-card" style="--preset-accent:${p.accent}"><div class="preset-top"><span>${esc(p.category)} · ${esc(p.industry)}</span><span class="dna-pill">${DNA_PROFILES[p.dna].label}</span></div><h3>${esc(p.title)}</h3><p>${esc(p.description)}</p><div class="preset-tags"><span>${esc(p.visual)}</span><span>I${p.intensity}/5</span><span>${esc(p.depth)}</span></div><div class="preset-actions"><button class="button button-primary" type="button" data-use="${p.id}">Usar preset</button><button class="button button-secondary" type="button" data-preview="${p.id}">DNA</button></div></article>`).join('');el.count.textContent=`${list.length} ${list.length===1?'preset':'presets'}`;el.empty.hidden=list.length>0;el.grid.hidden=list.length===0;el.load.parentElement.hidden=shown.length>=list.length}
function mapExperience(category){if(category==='Landing'||category==='Arquitectura'||category==='Restauración'||category==='Gaming')return'Landing page';if(category==='Aplicación')return'Aplicación web';return experiences.includes(category)?category:'Aplicación web'}
function usePreset(id,scroll=true){const p=presets.find(x=>x.id===id);if(!p)return;$('#projectInput').value=p.title;$('#experienceSelect').value=mapExperience(p.category);$('#industrySelect').value=p.industry;$('#stackSelect').value=p.stack;$('#visualSelect').value=p.visual;$('#dnaSelect').value=p.dna;$('#paceSelect').value=p.pace;$('#depthSelect').value=p.depth;el.intensity.value=p.intensity;el.intensityOut.textContent=`${p.intensity} / 5`;const boxes=$$('input[name="features"]',el.form);boxes.forEach(b=>b.checked=['page-transitions','scroll-story','microinteractions'].includes(b.value));if(p.depth==='spatial')$('input[value="3d-reactive"]',el.form).checked=true;if(p.dna==='kinetic')$('input[value="kinetic-type"]',el.form).checked=true;updateInspector();if(scroll)$('#studio').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion:reduce)').matches?'auto':'smooth'});toast(`Preset ${p.title} cargado.`)}

function formData(){const fd=new FormData(el.form);return{project:String(fd.get('project')||'').trim(),experience:String(fd.get('experience')),industry:String(fd.get('industry')),stack:String(fd.get('stack')),visual:String(fd.get('visual')),dna:String(fd.get('dna')),pace:String(fd.get('pace')),depth:String(fd.get('depth')),intensity:Number(fd.get('intensity'))||3,features:fd.getAll('features').map(String),context:String(fd.get('context')||'').trim()}}
function riskFrom(d){let perf=d.intensity*8,cog=d.intensity*7,a11y=d.intensity*4;if(d.depth==='layered')perf+=10;if(d.depth==='spatial'){perf+=26;cog+=8;a11y+=8}if(d.stack.includes('Three.js'))perf+=18;if(d.features.includes('3d-reactive'))perf+=22;if(d.features.includes('cursor-effects')){perf+=6;a11y+=12}if(d.features.includes('kinetic-type'))cog+=16;if(d.features.includes('scroll-story'))cog+=7;if(d.features.length>=5){perf+=10;cog+=12}if(d.dna==='cinematic')perf+=6;if(d.dna==='spatial')perf+=10;if(d.dna==='brutal')a11y+=5;if(d.pace==='fast'){cog+=11;a11y+=5}if(d.pace==='calm')cog-=5;perf=clamp(perf,4,100);cog=clamp(cog,4,100);a11y=clamp(a11y,4,100);const total=Math.round(perf*.42+cog*.34+a11y*.24);return{perf,cog,a11y,total}}
function recommendations(d,r){const rec=[];if(r.perf>60)rec.push('Reduce efectos simultáneos y limita animaciones a transform/opacity; pausa escenas fuera del viewport.');if(d.depth==='spatial')rec.push('Define fallback 2D para móvil, reduce pixel ratio y suspende 3D con pestaña oculta.');if(d.features.includes('cursor-effects'))rec.push('Desactiva efectos de cursor en touch y nunca los uses como único feedback.');if(d.features.includes('kinetic-type'))rec.push('Reserva tipografía cinética a titulares cortos; el cuerpo debe permanecer estático y seleccionable.');if(d.intensity>=4)rec.push('Limita a una animación protagonista por viewport y usa microinteracciones como soporte.');if(d.pace==='fast')rec.push('Asegura tiempos de lectura y evita encadenar transiciones antes de que el usuario recupere control.');if(!rec.length)rec.push('Configuración contenida: mantén una animación protagonista y usa el resto para orientación y feedback.');rec.push('Implementa siempre prefers-reduced-motion con equivalentes instantáneos, no simplemente duration: 0 en animaciones esenciales.');return rec.slice(0,5)}
function updateInspector(){const d=formData(),r=riskFrom(d),recs=recommendations(d,r);el.riskRing.style.setProperty('--score',r.total);el.riskScore.textContent=r.total;el.perfRisk.value=r.perf;el.cognitiveRisk.value=r.cog;el.a11yRisk.value=r.a11y;let level='BAJO',cls='risk-low',summary='Configuración equilibrada. El movimiento acompaña la jerarquía sin dominarla.';if(r.total>=65){level='ALTO';cls='risk-high';summary='Hay riesgo de sobreanimación o coste elevado. Simplifica antes de implementar.'}else if(r.total>=38){level='MEDIO';cls='risk-medium';summary='El sistema puede funcionar, pero necesita límites explícitos y fallback en móvil.'}el.riskBadge.className=`risk-badge ${cls}`;el.riskBadge.textContent=level;el.riskSummary.textContent=summary;el.recs.innerHTML=recs.map(x=>`<div class="recommendation">${esc(x)}</div>`).join('');return{risk:r,recs}}

function paceMs(pace,intensity){const base=pace==='calm'?620:pace==='fast'?280:420;return Math.round(base*(1+(intensity-3)*.07))}
function buildTimeline(d,profile){const unit=paceMs(d.pace,d.intensity),items=[];let t=0;const push=(name,detail,duration)=>{items.push({time:t,name,detail,duration});t+=duration};push('Page shell','Fondo, navegación y layout crítico visibles',Math.round(unit*.35));push('Hero eyebrow','Opacity + translate corto',Math.round(unit*.45));push('Hero headline',profile.enter,Math.round(unit*.85));push('Hero support','Texto y CTA con stagger moderado',Math.round(unit*.55));if(d.features.includes('scroll-story'))push('Scroll chapter 01','Revelado al 18–42% del viewport',Math.round(unit*1.2));if(d.features.includes('kinetic-type'))push('Kinetic headline','Sólo titular; detener al salir del viewport',Math.round(unit*.8));if(d.depth!=='flat')push('Depth layer','Parallax local ≤ 10–18 px; sin afectar lectura',Math.round(unit*1.05));if(d.features.includes('3d-reactive'))push('3D scene','Entrada progresiva; fallback y pause offscreen',Math.round(unit*1.4));push('Primary CTA feedback','Pressed 90–140 ms + confirmación visible',130);push('Page transition','Salida no bloqueante y restauración de foco',Math.round(unit*.75));return items}
function buildQa(d,r){const checks=[];const add=(status,title,detail)=>checks.push({status,title,detail});add('pass','Reduced motion obligatorio','La spec incluye una ruta equivalente para prefers-reduced-motion.');add(d.intensity<=3?'pass':'warn','Intensidad global',d.intensity<=3?'Dentro del rango recomendado para uso general.':'Reserva el nivel alto para momentos protagonistas.');add(r.perf<60?'pass':'warn','Presupuesto de animación',r.perf<60?'Riesgo técnico contenido.':'Reduce 3D, filtros, capas simultáneas o duración.');add(d.depth==='spatial'||d.features.includes('3d-reactive')?'warn':'pass','Fallback móvil',d.depth==='spatial'||d.features.includes('3d-reactive')?'Obligatorio: versión 2D/estática para dispositivos limitados.':'No se detecta dependencia fuerte de 3D.');add(d.features.includes('cursor-effects')?'warn':'pass','Entrada táctil',d.features.includes('cursor-effects')?'Cursor effects deben desactivarse en touch y teclado.':'No hay comportamiento dependiente del puntero.');add(d.features.includes('kinetic-type')?'warn':'pass','Legibilidad tipográfica',d.features.includes('kinetic-type')?'No animes párrafos, formularios ni contenido de lectura sostenida.':'La tipografía puede permanecer estable durante lectura.');add('pass','Propiedades de bajo coste','Priorizar transform y opacity; evitar top/left/width/height durante animación.');add('pass','Control del usuario','No usar scroll hijacking; focus, Escape y estados pressed deben seguir disponibles.');add('pass','Objetivo de fluidez','Diseñar para 60 fps y degradar efectos si el dispositivo no sostiene el presupuesto.');const warnings=checks.filter(x=>x.status==='warn').length;const score=clamp(100-warnings*7-Math.max(0,r.total-45)*.35,55,100);return{checks,score:Math.round(score)}}
function buildSpec(d,profile,r,recs,timeline,qa){const featureNames=d.features.length?d.features.join(', '):'microinteracciones esenciales';return`# ${d.project} — Motion Design System\n\nMotion 404 v${VERSION}\n\n## 1. MOTION DNA\n- Perfil: ${profile.label}\n- Carácter: ${profile.character}\n- Intensidad: ${d.intensity}/5\n- Ritmo: ${d.pace}\n- Profundidad: ${d.depth}\n- Easing principal: ${profile.easing}\n- Duración base: ${profile.duration}\n- Stagger: ${profile.stagger}\n- Entrada característica: ${profile.enter}\n- Hover/focus: ${profile.hover}\n- Scroll: ${profile.scroll}\n- Evitar: ${profile.avoid}\n\n## 2. CONTEXTO DE PRODUCTO\n- Experiencia: ${d.experience}\n- Sector: ${d.industry}\n- Stack: ${d.stack}\n- Dirección visual: ${d.visual}\n- Capacidades: ${featureNames}\n${d.context?`- Contexto adicional: ${d.context}\n`:''}\n## 3. TOKENS DE MOVIMIENTO\n\`\`\`css\n:root {\n  --motion-fast: 140ms;\n  --motion-base: ${paceMs(d.pace,d.intensity)}ms;\n  --motion-slow: ${Math.round(paceMs(d.pace,d.intensity)*1.65)}ms;\n  --motion-ease: ${profile.easing};\n  --motion-distance: ${8+d.intensity*3}px;\n  --motion-scale: ${d.intensity>=4?'1.035':'1.02'};\n}\n\n@media (prefers-reduced-motion: reduce) {\n  *, *::before, *::after {\n    scroll-behavior: auto !important;\n  }\n}\n\`\`\`\n\n## 4. REGLAS DEL SISTEMA\n1. Cada viewport tendrá como máximo una animación protagonista.\n2. Microinteracciones sólo comunican affordance, estado o continuidad.\n3. Animar transform y opacity siempre que sea posible.\n4. No bloquear click, teclado, lectura ni scroll durante una transición.\n5. Las animaciones decorativas deben poder eliminarse sin perder información.\n6. prefers-reduced-motion recibe una experiencia equivalente, no una versión rota.\n7. En móvil se reduce distancia, profundidad, blur y número de elementos animados.\n8. Toda escena pesada se pausa fuera del viewport y al ocultarse la pestaña.\n\n## 5. COMPONENT CONTRACT\n### Navigation\n- Entrada: ${profile.enter}.\n- Cambio de estado: ≤ 220 ms.\n- Focus visible no depende de movimiento.\n\n### Hero\n- Secuencia: eyebrow → headline → supporting copy → CTA.\n- Stagger recomendado: ${profile.stagger}.\n- La CTA debe ser interactuable antes de finalizar animaciones decorativas.\n\n### Cards / modules\n- Hover: ${profile.hover}.\n- Touch: sustituir hover por pressed/selected.\n- No mover layout al hacer hover.\n\n### Page transitions\n- Máximo recomendado: ${d.pace==='calm'?'650':'450'} ms antes de devolver control completo.\n- Restaurar foco tras navegación y respetar historial.\n\n## 6. TIMELINE BASE\n${timeline.map((x,i)=>`${i+1}. T+${x.time}ms — ${x.name}: ${x.detail} (${x.duration}ms)`).join('\n')}\n\n## 7. PERFORMANCE BUDGET\n- Riesgo estimado del sistema: ${r.total}/100.\n- GPU/CPU: ${r.perf}/100.\n- Carga cognitiva: ${r.cog}/100.\n- Riesgo accesibilidad: ${r.a11y}/100.\n- Objetivo: 60 fps; evitar long tasks durante interacción.\n- Evitar layout thrashing y listeners de scroll sin rAF/IntersectionObserver cuando aplique.\n- 3D/vídeo: carga progresiva, DPR limitado y fallback estático.\n\n## 8. ACCESSIBILITY CONTRACT\n- prefers-reduced-motion obligatorio.\n- Ninguna información depende sólo de movimiento.\n- Focus visible persistente durante y después de transiciones.\n- Nada debe desaparecer antes de poder ser leído con zoom 200%.\n- No usar parpadeos rápidos ni ciclos infinitos de alta atención.\n\n## 9. ANTI-OVERANIMATION\n${recs.map((x,i)=>`${i+1}. ${x}`).join('\n')}\n\n## 10. QA GATE — ${qa.score}/100\n${qa.checks.map(x=>`- ${x.status==='pass'?'PASS':'REVIEW'} — ${x.title}: ${x.detail}`).join('\n')}\n\n## 11. DEFINITION OF DONE\n- [ ] La jerarquía se entiende con animaciones desactivadas.\n- [ ] Se ha probado ratón, teclado y touch.\n- [ ] Se ha probado prefers-reduced-motion.\n- [ ] No hay scroll horizontal ni saltos de layout causados por motion.\n- [ ] No existen animaciones que bloqueen interacción.\n- [ ] Las escenas pesadas tienen fallback y pausa fuera de viewport.\n- [ ] La consola queda limpia durante navegación e interacción.\n- [ ] El rendimiento se valida en un móvil real de gama media.\n`}
function generateSystem(d){if(!d.project)return null;const profile=DNA_PROFILES[d.dna]||DNA_PROFILES.minimal;const{risk:r,recs}=updateInspector();const timeline=buildTimeline(d,profile);const qa=buildQa(d,r);const spec=buildSpec(d,profile,r,recs,timeline,qa);return{id:`sys-${Date.now()}`,created:new Date().toISOString(),project:d.project,input:d,profile:{key:d.dna,...profile},risk:r,recommendations:recs,timeline,qa,spec,output:{spec}}

function renderSystem(sys){currentSystem=sys;const d=sys.input,p=sys.profile;el.output.hidden=false;el.outputTitle.textContent=d.project;el.outputMeta.textContent=`${p.label} · intensidad ${d.intensity}/5 · ${d.stack} · QA ${sys.qa.score}/100`;el.dnaPanel.innerHTML=`<div class="dna-grid"><div class="dna-card"><small>Perfil</small><strong>${esc(p.label)}</strong></div><div class="dna-card"><small>Carácter</small><strong>${esc(p.character)}</strong></div><div class="dna-card"><small>Intensidad</small><strong>${d.intensity} / 5</strong></div><div class="dna-card"><small>Ritmo</small><strong>${esc(d.pace)}</strong></div><div class="dna-card"><small>Profundidad</small><strong>${esc(d.depth)}</strong></div><div class="dna-card"><small>Easing</small><strong>${esc(p.easing)}</strong></div><div class="dna-card"><small>Duration</small><strong>${esc(p.duration)}</strong></div><div class="dna-card"><small>Stagger</small><strong>${esc(p.stagger)}</strong></div></div><div class="dna-principles"><article><h4>Usar</h4><ul><li>${esc(p.enter)}</li><li>${esc(p.hover)}</li><li>${esc(p.scroll)}</li></ul></article><article><h4>Evitar</h4><ul><li>${esc(p.avoid)}</li>${sys.recommendations.slice(0,2).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article></div>`;el.spec.textContent=sys.spec;el.timeline.innerHTML=sys.timeline.map(x=>`<div class="timeline-item"><span class="timeline-time">T+${x.time}ms</span><strong>${esc(x.name)}</strong><span>${esc(x.detail)} · ${x.duration}ms</span></div>`).join('');el.qaScore.innerHTML=`<strong>${sys.qa.score}</strong><span>QA predictivo / 100</span>`;el.qaList.innerHTML=sys.qa.checks.map(x=>`<div class="qa-item qa-${x.status}"><span class="qa-icon">${x.status==='pass'?'✓':'!'}</span><div><strong>${esc(x.title)}</strong><br><small>${esc(x.detail)}</small></div><span>${x.status==='pass'?'PASS':'REVIEW'}</span></div>`).join('');selectTab('dna');el.output.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion:reduce)').matches?'auto':'smooth',block:'start'});el.outputTitle.focus({preventScroll:true})}
function selectTab(name){currentTab=name;$$('[role="tab"]',$('.tabs')).forEach(btn=>{const active=btn.dataset.tab===name;btn.setAttribute('aria-selected',String(active));btn.tabIndex=active?0:-1});['dna','spec','timeline','qa'].forEach(n=>{$(`#panel-${n}`).hidden=n!==name})}
function tabText(){if(!currentSystem)return'';if(currentTab==='dna')return`# ${currentSystem.project} — Motion DNA\n\n${JSON.stringify({profile:currentSystem.profile,input:currentSystem.input,risk:currentSystem.risk},null,2)}`;if(currentTab==='spec')return currentSystem.spec;if(currentTab==='timeline')return currentSystem.timeline.map(x=>`T+${x.time}ms — ${x.name}: ${x.detail} (${x.duration}ms)`).join('\n');return`QA ${currentSystem.qa.score}/100\n\n`+currentSystem.qa.checks.map(x=>`${x.status.toUpperCase()} — ${x.title}: ${x.detail}`).join('\n')}
async function copyText(text){try{await navigator.clipboard.writeText(text);toast('Copiado al portapapeles.')}catch{const t=document.createElement('textarea');t.value=text;t.style.position='fixed';t.style.opacity='0';document.body.append(t);t.select();document.execCommand('copy');t.remove();toast('Copiado al portapapeles.')}}
function download(filename,text,type='text/markdown'){const blob=new Blob([text],{type:`${type};charset=utf-8`});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),800)}

function saveCurrent(){if(!currentSystem)return;const item=JSON.parse(JSON.stringify(currentSystem));const existing=persisted.saved.findIndex(x=>x.project===item.project);if(existing>=0)persisted.saved.splice(existing,1);persisted.saved.unshift(item);persisted.saved=persisted.saved.slice(0,MAX_SAVED);const ok=saveState();renderSaved();toast(ok?'Motion System guardado localmente.':'Guardado sólo en memoria: localStorage no está disponible.')}
function renderSaved(){el.savedGrid.innerHTML=persisted.saved.map(s=>`<article class="saved-card"><small>${new Date(s.created).toLocaleDateString('es-ES')}</small><h3>${esc(s.project)}</h3><p>${esc(s.profile.label)} · I${s.input.intensity}/5 · QA ${s.qa.score}/100</p><div class="saved-actions"><button class="button button-secondary" type="button" data-load-system="${esc(s.id)}">Abrir</button><button class="button button-ghost" type="button" data-delete-system="${esc(s.id)}">Eliminar</button></div></article>`).join('');el.savedEmpty.hidden=persisted.saved.length>0}
function exportSystems(){const payload={app:'Motion 404',version:VERSION,exportedAt:new Date().toISOString(),saved:persisted.saved};download(`motion-404-v2-systems-${new Date().toISOString().slice(0,10)}.json`,JSON.stringify(payload,null,2),'application/json');toast('Sistemas exportados.')}
function importSystems(file){if(!file||file.size>MAX_IMPORT_BYTES){toast('JSON no válido o demasiado grande.');return}const reader=new FileReader();reader.onerror=()=>toast('No se pudo leer el archivo.');reader.onload=()=>{try{const data=JSON.parse(String(reader.result));if(data.app!=='Motion 404'||!Array.isArray(data.saved))throw new Error();const safe=data.saved.filter(validSaved).slice(0,MAX_SAVED);persisted.saved=safe;saveState();renderSaved();toast(`${safe.length} sistemas importados.`)}catch{toast('El archivo no es una copia válida de Motion 404 v2.')}};reader.readAsText(file)}
function applyTheme(){document.documentElement.dataset.theme=persisted.theme;document.querySelector('meta[name="theme-color"]').content=persisted.theme==='light'?'#f5f5f2':'#080a0f'}

function bind(){['input','change'].forEach(evt=>el.form.addEventListener(evt,e=>{if(e.target===el.intensity)el.intensityOut.textContent=`${el.intensity.value} / 5`;updateInspector()}));el.form.addEventListener('submit',e=>{e.preventDefault();const d=formData();if(!d.project){$('#projectInput').focus();toast('Pon un nombre al proyecto.');return}renderSystem(generateSystem(d))});el.form.addEventListener('reset',()=>setTimeout(()=>{el.intensityOut.textContent='3 / 5';updateInspector()},0));$('#inspectButton').addEventListener('click',()=>{updateInspector();toast('Riesgo recalculado con la configuración actual.')});el.grid.addEventListener('click',e=>{const use=e.target.closest('[data-use]'),preview=e.target.closest('[data-preview]');if(use)usePreset(use.dataset.use);if(preview){usePreset(preview.dataset.preview,false);const sys=generateSystem(formData());renderSystem(sys)}});[el.search,el.category,el.dnaFilter].forEach(x=>x.addEventListener(x===el.search?'input':'change',()=>{visible=PAGE_SIZE;renderPresets()}));el.load.addEventListener('click',()=>{visible+=PAGE_SIZE;renderPresets()});$('#clearPresetFilters').addEventListener('click',()=>{el.search.value='';el.category.value='all';el.dnaFilter.value='all';visible=PAGE_SIZE;renderPresets()});$('#randomPreset').addEventListener('click',()=>{const p=presets[Math.floor(Math.random()*presets.length)];usePreset(p.id)});document.addEventListener('keydown',e=>{if(e.key==='/'&&!/input|textarea|select/i.test(document.activeElement.tagName)){e.preventDefault();el.search.focus()}if(['ArrowLeft','ArrowRight','Home','End'].includes(e.key)&&e.target.matches('[role="tab"]')){e.preventDefault();const tabs=$$('[role="tab"]',$('.tabs'));let i=tabs.indexOf(e.target);if(e.key==='Home')i=0;else if(e.key==='End')i=tabs.length-1;else i=(i+(e.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;tabs[i].focus();selectTab(tabs[i].dataset.tab)}});$('.tabs').addEventListener('click',e=>{const b=e.target.closest('[data-tab]');if(b)selectTab(b.dataset.tab)});$('#copyOutput').addEventListener('click',()=>copyText(tabText()));$('#downloadOutput').addEventListener('click',()=>{if(currentSystem)download(`${slug(currentSystem.project)}-motion-system.md`,currentSystem.spec)});$('#saveSystem').addEventListener('click',saveCurrent);$('#exportSystems').addEventListener('click',exportSystems);$('#importSystems').addEventListener('change',e=>{importSystems(e.target.files[0]);e.target.value=''});$('#clearSystems').addEventListener('click',()=>{if(!persisted.saved.length)return;if(confirm('¿Borrar todos los Motion Systems guardados en este navegador?')){persisted.saved=[];saveState();renderSaved();toast('Sistemas guardados eliminados.')}});el.savedGrid.addEventListener('click',e=>{const load=e.target.closest('[data-load-system]'),del=e.target.closest('[data-delete-system]');if(load){const s=persisted.saved.find(x=>x.id===load.dataset.loadSystem);if(s){renderSystem(s);$('#studio').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion:reduce)').matches?'auto':'smooth'})}}if(del){persisted.saved=persisted.saved.filter(x=>x.id!==del.dataset.deleteSystem);saveState();renderSaved();toast('Sistema eliminado.')}});$('#themeButton').addEventListener('click',()=>{persisted.theme=persisted.theme==='dark'?'light':'dark';saveState();applyTheme()})}

function init(){fillOptions();applyTheme();renderPresets();renderSaved();bind();updateInspector();console.info(`Motion 404 v${VERSION} · ${presets.length} presets · local-first`)}
init();